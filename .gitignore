# ----------------------------------------------------------------------------
# Build Artifacts
# ----------------------------------------------------------------------------
# The target directory contains all build artifacts and dependencies.
# It is heavy and should never be committed.
/target/

# ----------------------------------------------------------------------------
# Cargo Files
# ----------------------------------------------------------------------------
# For a Rust binary (application), Cargo.lock SHOULD be committed to Git.
# It ensures reproducible builds by locking dependency versions.
# Only uncomment the line below if you have a specific reason to ignore it.
# Cargo.lock

# ----------------------------------------------------------------------------
# Environment Variables & Secrets (Critical for Binaries)
# ----------------------------------------------------------------------------
# Ignore actual environment files containing secrets/API keys.
.env
.env.local
.env.*.local

# It is common practice to commit a template file without secrets.
# !.env.example

# ----------------------------------------------------------------------------
# Crash Dumps & Backups
# ----------------------------------------------------------------------------
# Backup files generated by rustfmt when formatting fails
**/*.rs.bk

# MSVC debug program database files
*.pdb

# ----------------------------------------------------------------------------
# Logs
# ----------------------------------------------------------------------------
*.log
/logs/

# ----------------------------------------------------------------------------
# OS Generated Files
# ----------------------------------------------------------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Linux
*~

# ----------------------------------------------------------------------------
# IDE & Editor Configuration
# ----------------------------------------------------------------------------
# JetBrains (IntelliJ, CLion, RustRover)
.idea/

# Visual Studio Code
# Note: It is often useful to commit .vscode/ for shared launch configurations.
# Uncomment the line below if you wish to ignore workspace settings.
# .vscode/

# Vim / Emacs temporary files
*.swp
*.swo